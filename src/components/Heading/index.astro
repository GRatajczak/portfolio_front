---
import type { Props } from "./index.type";

const { as = "h2", text = "", className = "", style = "" } =
    Astro.props as Props;
const Tag = as;
---

<Tag class={className} style={style}>
    <slot>{text}</slot>
</Tag>
