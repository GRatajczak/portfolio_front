export type SubheadlineItem = {
    text?: string;
};

export type Props = {
    headline?: string;
    subheadline?: SubheadlineItem[];
    facebook?: string;
    instagram?: string;
    phone?: string;
};
